# ─────────────────────────────────────────────────────────────
# 이 파일을 .env.local 로 복사해서 값을 채운다. (.env* 는 추적 제외)
#
# 주의: VITE_ 접두사가 붙은 값은 전부 번들에 박혀 공개된다.
#       anon key 는 공개돼도 되지만(인가는 Postgres RLS 가 담당),
#       service_role 키는 절대 여기에 넣지 않는다.
# ─────────────────────────────────────────────────────────────

# 데이터 어댑터: mock | supabase
# mock 이면 Supabase 프로젝트 없이도 앱 전체가 돌고, supabase-js 는 빌드에서 통째로 빠진다
# (mock 310KB / supabase 526KB — 빌드 타임 상수라 트리셰이킹됨)
VITE_DATA_SOURCE=mock

# Supabase
# URL 은 대시보드 > Project Settings > Data API
# anon key 는 번들에 노출돼도 되는 값이다 — 인가는 Postgres RLS 가 한다.
# service_role 키는 RLS 를 우회하므로 여기에도, Vercel 환경변수에도 넣지 않는다.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# 디자인 대조용 날짜 고정 (KST). 비워두면 실제 오늘 날짜를 쓴다.
# 프로토타입 기준일과 맞추려면 2026-08-20
VITE_MOCK_TODAY=

# 배포 시 Vercel 이 커밋 SHA 를 주입한다 (쿼리 캐시 buster 로 사용)
VITE_APP_VERSION=dev
